name: Product CI Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

env:
  NODE_OPTIONS: '--no-deprecation' # Khắc phục cảnh báo DeprecationWarning: The `punycode` module is deprecated trên Node mới

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # BẮT BUỘC: Thiết lập sử dụng NodeJS v24 làm phiên bản chạy mặc định mới của runner
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24.x'
          cache: 'npm'
          cache-dependency-path: |
            frontend/package-lock.json
            backend/package-lock.json

      # --- KIỂM THỬ VÀ BUILD FRONTEND ---
      - name: Install Frontend Dependencies
        working-directory: frontend
        run: |
          npm ci
          # LƯU Ý: Khắc phục lỗi thiếu native binding của build tool (Vite/Esbuild) trên runner Linux
          npm install @esbuild/linux-x64 --no-save || true

      - name: Run Frontend Tests
        working-directory: frontend
        run: npm run test -- --run

      - name: Build Frontend
        working-directory: frontend
        run: npm run build

      # --- KIỂM THỬ BACKEND ---
      - name: Install Backend Dependencies
        working-directory: backend
        run: npm ci

      - name: Run Backend Tests
        working-directory: backend
        run: npm run test -- --run
